{
  "name": "interpreter",
  "version": "1.0.0",
  "type": "module",
  "repository": "git@github.com:linvain/interpreter.git",
  "author": "linvain <linvain@protonmail.com>",
  "license": "MIT",
  "scripts": {
    "start": "babel src --out-dir dist && node dist",
    "test": "babel test --out-dir test-dist && jest test-dist",
    "lint": "eslint 'src/**/*.js'"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "eslint": "^6.3.0",
    "eslint-plugin-import": "^2.18.2",
    "jest": "^24.9.0"
  }
}
